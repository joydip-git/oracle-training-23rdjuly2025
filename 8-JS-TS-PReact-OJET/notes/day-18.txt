context
redux

	
passing data from one component to another

1. immediate nested components
2. deeply nested components
	a. context [deeply nested component in a tree]
	b. application-wide state management [ any component can share data]
	c. rxjs
		- observable
		- observer
		- map, of, reduce, filter etc.

OJET
	- Oracle JavaScript Extension Toolkit

	- MVVM (Model-View-ViewModel) - KnockOut JS, Angular
	- VDOM (Virtual DOM) - VDOM, Preact (React)

OJET CLI installation:
npm install --location=global @oracle/ojet-cli

crate web app using VDOM:
ojet create <app-name> --template=basic --vdom









